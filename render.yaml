# Render Blueprint — MF FAQ Chatbot API
# https://render.com/docs/blueprint-spec
#
# Deploy: Dashboard → New → Blueprint → select this repo → apply.
# Set secret values in the Render UI for every env var with sync: false.

services:
  - type: web
    name: mf-faq-api
    runtime: python
    region: oregon
    # Free tier ($0) — spins down after ~15 min idle; ~1 min cold start. See:
    # https://render.com/docs/free
    plan: free
    branch: main
    # Render defaults new services to Python 3.14+; greenlet/tiktoken need 3.11.x wheels.
    # https://render.com/docs/python-version — PYTHON_VERSION overrides the default.
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: uvicorn src.api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
      # Secrets — Render prompts for these on first apply (or set under Environment).
      - key: GROQ_API_KEY
        sync: false
      - key: PINECONE_API_KEY
        sync: false
      # Optional: add PINECONE_HOST in the dashboard if you use host-based Pinecone.
      # Optional non-defaults (edit in dashboard if different).
      - key: GROQ_MODEL
        value: llama-3.1-8b-instant
      - key: PINECONE_INDEX
        value: mf-faq-chunks
      - key: PINECONE_NAMESPACE
        value: mf-faq-active
      - key: PINECONE_CLOUD
        value: aws
      - key: PINECONE_REGION
        value: us-east-1
      - key: EMBEDDING_MODEL
        value: llama-text-embed-v2
      # Cross-encoder is heavy; keep off on small instances unless you need it.
      - key: ENABLE_CROSS_ENCODER
        value: "0"
