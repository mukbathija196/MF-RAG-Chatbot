- [Done] Factual-answer quality fix: replaced generic fallback with value-extraction fallback from top reranked chunk(s) (e.g., exit load / expense ratio / SIP / benchmark / risk sentence) so responses contain an actual fact even if LLM call fails.
- [Done] LLM-call observability: added logging around Groq generation path (`Calling Groq model=...`) and warning logs on fallback, so it is clear whether the app used LLM output or source-text fallback.
- [Done] Structured holdings extraction: parser now writes explicit holdings records to `data/processed/holdings_records.jsonl` (holding name, sector, instrument, weight), and retrieval injects a structured holdings summary candidate so the chatbot can answer top holdings and sector-weight queries more reliably.
- [Done] Structured holdings extraction: parser now writes explicit holdings records to `data/processed/holdings_records.jsonl` (holding name, sector, instrument, weight), and retrieval injects a structured holdings summary candidate so the chatbot can answer top holdings and sector-weight queries more reliably.
- [Done] Returns/CAGR guardrail refinement: performance detector now allows factual scheme-level returns/CAGR questions (for example, "What is the 3Y return of Nippon India Large Cap Fund?") and blocks only projection/comparison asks (for example, "compare returns", "if I invest...", "how much profit", or "calculate XIRR/CAGR").
- [Done] Structured returns/CAGR extraction: parser now pulls `return_stats` (1D/1W/1M/3M/6M/1Y/3Y/5Y/10Y/since inception) from each scheme's Groww `__NEXT_DATA__` payload into `data/processed/returns_records.jsonl`. Retriever injects a structured returns summary candidate and the generator deterministically extracts the specific period requested (fixes Multi Cap returning the 1D change for 3Y/5Y queries, and fixes empty/incorrect answers for 5Y/10Y/since inception across all schemes).